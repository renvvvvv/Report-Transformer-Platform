# 此文件由平台API动态生成和管理
# 每个生成的报表服务都会在这里注册一个location

# 示例格式：
# location /reports/{service-name}/ {
#     proxy_pass http://{service-container}:5000/;
#     proxy_set_header Host $host;
#     proxy_set_header X-Real-IP $remote_addr;
#     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     proxy_set_header X-Forwarded-Prefix /reports/{service-name};
# }
